# my-translator
- My language translator in python

Requirements(run in your terminal):
```
pip install googletrans==3.1.0a0
```
or a newer version
```
pip install googletrans
```
[Access the documentation](https://py-googletrans.readthedocs.io/en/latest/#googletrans-free-and-unlimited-google-translate-api-for-python])
